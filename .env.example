# Document to Anki CLI Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# LLM Configuration (Required)
# =============================================================================

# Gemini API Key (Required for AI flashcard generation)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative: Use OpenAI API (if you prefer GPT models)
# OPENAI_API_KEY=your_openai_api_key_here

# LLM Model Configuration
# Default model for flashcard generation
MODEL=gemini/gemini-pro
# Alternative models:
# MODEL=openai/gpt-4
# MODEL=openai/gpt-3.5-turbo

# =============================================================================
# Application Settings
# =============================================================================

# Flashcard Language Configuration
# Language for generated flashcard content (questions and answers)
CARDLANG=english
# Supported languages and formats:
# - English: "english" or "en"
# - French: "french" or "fr" 
# - Italian: "italian" or "it"
# - German: "german" or "de"
# 
# Examples:
# CARDLANG=english    # Generate flashcards in English (default)
# CARDLANG=fr         # Generate flashcards in French
# CARDLANG=italian    # Generate flashcards in Italian
# CARDLANG=de         # Generate flashcards in German
#
# Default behavior: If CARDLANG is not set or empty, defaults to English
# Note: This affects the language of generated flashcard content, not the UI language

# Logging Configuration
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Enable verbose logging for debugging
VERBOSE=false

# Maximum file size for uploads (in MB)
MAX_FILE_SIZE_MB=50

# Maximum number of files to process in batch
MAX_BATCH_SIZE=10

# =============================================================================
# Web Interface Settings
# =============================================================================

# Web server configuration
WEB_HOST=0.0.0.0
WEB_PORT=8000

# Enable development mode (auto-reload)
WEB_DEBUG=false

# Session secret key (generate a secure random string)
SECRET_KEY=your_secret_key_here_change_this_in_production

# =============================================================================
# LLM Processing Settings
# =============================================================================

# Maximum tokens per LLM request
MAX_TOKENS_PER_REQUEST=4000

# Number of flashcards to generate per text chunk
FLASHCARDS_PER_CHUNK=10

# Retry configuration for LLM API calls
LLM_MAX_RETRIES=3
LLM_RETRY_DELAY=1

# Request timeout in seconds
LLM_TIMEOUT=30

# =============================================================================
# File Processing Settings
# =============================================================================

# Supported file extensions (comma-separated)
SUPPORTED_EXTENSIONS=.pdf,.docx,.txt,.md

# Temporary directory for file processing
TEMP_DIR=/tmp/document_to_anki

# Clean up temporary files after processing
CLEANUP_TEMP_FILES=true

# =============================================================================
# Export Settings
# =============================================================================

# Default output directory for CSV exports
OUTPUT_DIR=./exports

# CSV export format
CSV_DELIMITER=,
CSV_ENCODING=utf-8

# Include metadata in CSV export
INCLUDE_METADATA=true

# =============================================================================
# Development Settings (for developers only)
# =============================================================================

# Disable telemetry and tracking
DISABLE_TELEMETRY=true

# Enable development features
DEV_MODE=false

# Mock LLM responses for testing (use with caution)
MOCK_LLM_RESPONSES=false

# =============================================================================
# Performance Settings
# =============================================================================

# Number of worker processes for parallel processing
WORKER_PROCESSES=4

# Memory limit per worker (in MB)
MEMORY_LIMIT_MB=512

# Enable caching of processed content
ENABLE_CACHING=true

# Cache directory
CACHE_DIR=./.cache

# Cache expiration time (in hours)
CACHE_EXPIRATION_HOURS=24